import { ModulePlaceholder } from "@shared/components/ModulePlaceholder";

export function AgentHome() {
  return (
    <ModulePlaceholder
      module="Agent"
      phase="Phase 7"
      description="Cleaner job queue, navigation, status updates, before/after photo uploads, and area-alert triggers will live here."
    />
  );
}
